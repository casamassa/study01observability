global:
  scrape_interval: 5s # Busca dados a cada 5 segundos

scrape_configs:
  - job_name: "my-api-dotnet"
    static_configs:
      - targets: ["host.docker.internal:5000"] # Endereço da sua API
        labels:
          application: "my-api-dotnet"
